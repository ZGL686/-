# 0.1.0 初版验收

日期：2026-09-20。环境：Windows x64、Microsoft Edge / WebView2、Node 24、Rust MSVC。

| 检查 | 结果 |
| --- | --- |
| TypeScript 与 Vite 生产构建 | 通过 |
| 业务单元测试 | 11 / 11 通过 |
| 浏览器端到端场景 | 4 / 4 通过 |
| Rust 数据保存测试 | 2 / 2 通过 |
| 桌面独立数据目录测试 | 通过 |
| 桌面原生保存对话框与 JSON 文件读回 | 通过 |
| 1440×1000 与 1100×760 界面 | 人工截图检查，无横向页面溢出 |
| 发行 EXE 首次启动、正常退出 | 通过，64 位学生、0 条考勤、初始版本 1 |
| 压缩包与解压版 EXE 一致性 | SHA-256 一致 |
| 发行版调试端口 | 未开启 |

业务测试涵盖北京时区跨日、周一切周、第 3 周停课、14 周课程替换、时段匹配、备份往返与篡改拒绝、无效日期/重复学生拒绝、恢复副本不覆盖、撤销计数、CSV 引号与 Excel 学号前导零。

浏览器场景涵盖课表到考勤、登记后刷新、个人明细修改、撤销恢复、修改开学日期、新名单工作台、备份往返、Excel/Markdown 导出、损坏备份拒绝、两个窗口发生写入冲突时保护已有数据。

桌面测试在隔离目录中载入个人名单，登记一条测试记录，重新加载与退出重启后检查 SQLite 中的记录和两个历史版本；通过 Windows 原生保存对话框导出 JSON，再读回确认记录内容。测试数据未写入默认用户工作台。

最终交付文件：`dist/GuiLu-latest/归录.exe` 与 `dist/GuiLu-latest.zip`。包内还含使用说明和版本信息。EXE SHA-256：

```text
1C7DAC4CD65CC25E624EFE0A974666EB121B7C0B4EB8F02157803E863C5D89CC
```

本轮不承诺硬盘故障情况下绝不丢失。数据以独立目录、SQLite 事务、历史快照和外部备份降低风险。未进行完整安全审计；npm 生产依赖审计报告 ExcelJS→uuid 的两个中等风险条目，涉及 v3/v5/v6 外部缓冲区参数，所检 ExcelJS 源码使用 v4。初版未做商业代码签名。
