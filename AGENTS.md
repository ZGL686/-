# Ludian 工程协作规范

## 会话入口

1. 阅读本文件与 `wiki_memory/AGENTS.md`。
2. 按模板协议读取 `wiki_memory/当前状态/项目概览.md`、`系统架构.md`、`当前约束.md`、`当前待办.md`。
3. 按需读取相关决策、知识页，历史只读取最近相关的 1–3 篇日志。
4. 原代码、用户需求和测试结果是事实来源；`proposed` 页面不能作为用户已确认的决策。

## 用户已明确的约束

- React + Vite + Tauri；提供网页热更新开发与 Windows `.exe`。
- `dist` 保留最新压缩包和对应解压目录，最终验收解压版。
- 开学日期随时可改，课表依据教学周动态切换；考勤明细保留日期和课程，支持人工调整。
- 考勤数据需要跨程序版本保留，支持完整备份和恢复。
- 应用名 Ludian，黑白图标；左上角采用单一班级入口，提供风格明显的字体选项和清晰克制的微交互。
- 从整体维护代码质量与公共模块，避免重复实现和持续膨胀的页面文件。
- 每次完整对话产生的修改都必须提交并推送到 `origin`，不使用强制推送。

## 工程操作

- 学生真实名单、个人备份、SQLite 文件、屏幕截图、个人发行文件不提交 Git。
- 绝不把默认课表/名单重新灌入已有数据库；禁止无备份的破坏性迁移，版本升级必须兼容旧数据或明确拒绝写入。
- 固定应用标识 `cn.guilu.attendance`；若需要变更必须提供迁移方案。
- 自动化测试用虚构学生；桌面测试同时指定全新 `GUILU_DATA_DIR` 和 `WEBVIEW2_USER_DATA_FOLDER`，先校验实际目录，不得测试真实用户数据。
- 只有持久化成功才向用户显示保存成功；保留乐观版本检查及事务边界。
- 不自动将未登记视为出勤，不据此计算出勤率。
- 运行 `npm run check` 检查代码质量与模块边界；适合修改范围的测试包括 `npm test`、`npm run test:e2e`、`npm run build`；Rust 改动运行 Cargo 测试。
- 完整发布使用 `npm run release`。确认最终 EXE、ZIP 内容一致，更新使用说明和日志后提交推送。
- 参考模板中的长期记忆晋升规则，用户尚未确认的实现选型保留为 `proposed`；已明确的用户需求可以记录为 active。

## 记忆维护

完成实质任务后新增 `wiki_memory/日志/YYYY-MM-DD-任务标题.md`，包含实现、验证、结果与待确认长期记忆。不要写入学生信息、密钥、完整会话或内部推理。日志封存后只追加更正。

```powershell
python wiki_memory/工具/memory_lint.py index
python wiki_memory/工具/memory_lint.py check
```
