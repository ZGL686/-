---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-09-22
topic: ludian-release-rebuild
source_logs:
  - "[[日志/2026-09-20-Ludian交互与模块化改造]]"
supersedes: null
---

# 2026-09-22｜Ludian 正式包重建

- 类型：maintenance
- 状态：完成 0.3.0 正式包重建与解压版验收
- 目标：重新生成 `dist/Ludian-latest` 与 `dist/Ludian-latest.zip`，确认压缩包、解压目录和正式 Windows 程序可交付。
- 日志索引：[[日志/MOC_工作日志]]

## 实施

- 工作区开始时无源码改动；版本保持 0.3.0，应用标识保持 `cn.guilu.attendance`。
- 本机缺少 MSVC 链接器和 Windows SDK，安装 Microsoft Visual Studio 2022 Build Tools 的 C++ 工作负载后，在 x64 开发环境中执行 `npm run release`。
- 更新 `docs/使用说明.md` 的本次 EXE 校验值与 `docs/验收记录-0.3.0.md` 的重建记录，再以既有打包脚本重新封装最终目录和 ZIP。
- `dist/`、`web-dist/`、隔离测试数据及个人数据继续由 Git 忽略；未向发行包写入学生名单、数据库或备份。

## 验证

- ESLint 与 50 模块架构边界检查通过；TypeScript 通过。仓库内容符合 Prettier，默认检查仅因 Windows 工作树 CRLF 与配置默认 LF 不同而退出，使用 `--end-of-line crlf` 复核全部通过，未批量重写源码。
- Vitest 30 项通过；Playwright 17 项断言全部通过。Playwright 完成断言后测试壳未自动退出，确认测试服务端口已关闭后终止残留壳进程。
- Rust 单元测试 3 项通过。
- 正式前端、Rust release、EXE 与 ZIP 封装通过。ZIP 四个文件与解压目录逐字节一致，Windows 产品名、描述和 0.3.0 版本正确。
- 解压版在全新的 `GUILU_DATA_DIR` 与 `WEBVIEW2_USER_DATA_FOLDER` 中完成原生验收：旧快照保留、数据升级与保存、备份导出、离线字体、侧栏和外观偏好重启均通过。

## 结果

- 最终 EXE：`dist/Ludian-latest/Ludian.exe`，43,088,896 字节，SHA-256 `D652DD59F850275C8CFE149172EF6B71215497137528046BAF558F993B9682A6`。
- 最终 ZIP：`dist/Ludian-latest.zip`，34,637,254 字节，SHA-256 `8E8A831F84173F1521EC630C6BD83278C2358BA87F70EE861B554211BF04B071`。
- 解压版是最终验收对象；发行物继续保持 Git 忽略，不提交个人发行文件。

## 待确认长期记忆

- 无新增产品或架构决策。本次仅重建既有 0.3.0 发布物，不提升候选架构状态。
